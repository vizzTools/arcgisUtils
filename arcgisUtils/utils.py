# import arcgis

def get_sdf(server=None, data_table=None, creds=None):  
    """Fetches an item and returns an SDF"""

    print(f"Fetching SDF from {server+data_table}")

    return [{
        'key': 'value'
    }]

    